# Modules
This folder contains larger systems that require many scripts or have some interconnectedness. When using [UInit](https://github.com/ErencanPelin/UInit)
the folders in this directory are imported as-is into your local `/Scripts` folder, containing their own assembly.