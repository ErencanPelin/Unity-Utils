# Tools
This folder contains editor only scripts that perform small tooling actions to simplify mundane tasks, OR solve
common issues when integrating Unity with CI/CD deployment pipelines. When using [UInit](https://github.com/ErencanPelin/UInit)
the scripts in this directory are imported as-is into a `<Project Root>/.editor-tools` folder.